edition: 1.0.0
services:
  website:
    component: /Users/shihuali/workspace/website/lib/index.js # 指向项目路径
    access: private
    props:
      bucket: shl-test05
      src:
        src: './src'
        dist: './build'
        hook: npm run build
        index: index.html
        error: index.html
      region: cn-hangzhou
      hosts:
        - host: shl01.alijam.top

      # cors:
      #   allowedOrigin: '*'
      #   allowedMethod: ['GET']
      # referer:
      #   allowEmpty: true
      #   referers: ['100.67.79.188', '140.205.215.168']
